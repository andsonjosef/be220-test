export class SlideModel {
  title: string;
  /**
   * default: white
   */
  titleColor?: string;
  showSubTitle?: boolean;
  imgUrl?: string;
  /**
   * default: continuar treinando
   */
  subtitle?: string;
  subtitleColor?: string;
}
